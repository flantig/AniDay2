export declare class BotGuildUpdater {
    /**
     * Automatically refreshes the commands for immediate use upon joining a new guild/server.
     *
     * @param guild
     * @param client
     * @private
     */
    private botJoins;
    /**
     * Deletes any set auto timer for the discord bot.
     *
     * @param guild
     * @param client
     * @private
     */
    private botLeaves;
}
